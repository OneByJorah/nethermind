# AGENT_LOG — nethermind

## Phase 0 — Intake
- Stack: FastAPI backend + Next.js frontend + Docker Compose (identical architecture to hermes-switch-manager, which this repo was merged into).
- Status: **DEPRECATED** — all unique features (serial console, Jinja2 template engine, 45+ templates) consolidated into `hermes-switch-manager` (already polished in batch #6).
- No screenshots, no capture-screenshots.py (clean). `DEPRECATED.md` exists but README didn't reference it at the top.

## Phase 1–6 — Skipped (deprecated)
- App is a subset of hermes-switch-manager (already verified + polished in #6). Running it here would be redundant.
- No fake screenshots or claims to fix; repo is a consolidation artifact.

## Phase 5 — README
- Added a ⚠️ DEPRECATED banner at the very top linking to hermes-switch-manager + DEPRECATED.md.

## Status: DONE (deprecated — added top-of-README deprecation notice linking to successor repo #6)